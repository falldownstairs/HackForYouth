<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="initial-scale=1, width=device-width" />


    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap"
    />
  </head>
  <body>
    <div class="hero-actions">
      <!-- <img class="img-icon" alt="" src="../../public/pictures/venue.jpg" /> -->
      <div class="blue"/>
      <section class="text-content-title">
        <h1 class="title">Hack4Youth</h1>
        <h1 class="subtitle">A hackathon organized by students for students</h1>
      </section>
      <button class="button">
        <a href="https://jv3wjg8e4fu.typeform.com/to/BdhCnZ9Y" target = "_blank">
        <div class="sign-up">Sign Up</div>
        </a>
      </button>
    </div>
  </body>
</html>

<style>
  body {
  margin: 0;
  line-height: normal;
  background-size: cover;
}

:root {
  /* fonts */
  --subtitle: Inter;

  /* font sizes */
  --subtitle-size: 2rem;
  --title-hero-size: 4.5rem;

  /* Colors */
  --color-gray: #001f3a;
  --device-width: 75rem;
}


.subtitle,
.title {
  margin: 0;
  align-self: stretch;
  position: relative;
  line-height: 120%;
  font-family: inherit;
}
.title {
  font-size: 120px;
  letter-spacing: -0.03em;
}
.subtitle {
  padding-bottom: 25px;
  font-size: 40px;
  font-weight: 500;
  color: #8fcdff;
}
.text-content-title {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  gap: var(--space-200);
  text-align: center;
  font-size: var(--title-hero-size);
  color: #a5d7ff;
  font-family: var(--single-line-body-base);
}


.hero-actions {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  background-size: cover;
}
.button{
  width: 15rem;
  gap: var(--space-400);
  z-index: 100;
  border-radius: 10px;
}
.hero-actions {
  width: 100%;
  position: relative;
  background-color: #001f3a;
  max-width: 100%;
  flex-direction: column;
  padding-top: 250px;
  padding-bottom: 300px;
  box-sizing: border-box;
  gap: var(--space-800);
  text-align: left;
  font-size: var(--single-line-body-base-size);
  color: var(--text-default-default);
  font-family: var(--single-line-body-base);
}
.blue,
.img-icon {
  margin: 0 !important;
  position: absolute;
}
.img-icon {
  width: 100%;
  height: 100%;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  max-width: 100%;
  max-height: 100%;
  object-fit: cover;
}
.blue {
  background-color: #001f3ace;
  width: 100%;
  height: 100%;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  max-width: 100%;
  overflow: hidden;
  max-height: 100%;
  object-fit: cover;
}
.sign-up {
  position: relative;
  font-size: 1.25rem;
  line-height: 100%;
  font-family: var(--subtitle);
  color: var(--text-default-default);
  text-align: left;
  display: inline-block;
  min-width: 4.75rem;
}
.actions,
.button{
  display: flex;
  align-items: flex-start;
}
.button {
  cursor: pointer;
  border: 1px solid #024d8a;
  padding: 0.5rem 5.062rem;
  background-color: #75bcf6;
  height: 2.5rem;
  border-radius: var(--radius-200);
  box-sizing: border-box;
  overflow: hidden;
  flex-direction: row;
  justify-content: center;
  border-radius: 8px;
}
.actions{
  max-width: 100%;
}
.actions {
  width: 30.25rem;
  flex-direction: row;
  justify-content: flex-end;
}
</style>